services:

  api-rest-gateway:
    build:
      context: .
      dockerfile: services/api-rest-gateway/Dockerfile
    container_name: api-rest-gateway
    secrets:
      - auth-key
    environment:
      - USER-BASE_ENDPOINT=user-base-service:50051
      - FRIEND-REQUEST-BASE_ENDPOINT=friend-request-base-service:50052
      - AUTH-BASE_ENDPOINT=auth-base-service:50053
    ports:
      - "50053:8080"
    depends_on:
      - user-base-service
      - friend-request-base-service

  conversation-base-service:
    build:
      context: .
      dockerfile: services/conversation-base/Dockerfile
    container_name: conversation-base-service
    ports:
      - "50057:50051"
    secrets:
      - db-password
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=database
      - POSTGRES_SERVICE=db
      - POSTGRES_PORT=5432
    depends_on:
      db:
        condition: service_healthy

  user-base-service:
    build:
      context: .
      dockerfile: services/user-base/Dockerfile
    container_name: user-base-service
    ports:
      - "50051:50051"
    secrets:
      - db-password
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=database
      - POSTGRES_SERVICE=db
      - POSTGRES_PORT=5432
    depends_on:
      db:
        condition: service_healthy

  friend-request-base-service:
    build:
      context: .
      dockerfile: services/friend-request-base/Dockerfile
    container_name: friend-request-base-service
    ports:
      - "50052:50052"
    secrets:
      - db-password
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=database
      - POSTGRES_SERVICE=db
      - POSTGRES_PORT=5432
    depends_on:
      db:
        condition: service_healthy

  auth-base-service:
    build:
      context: .
      dockerfile: services/auth-base/Dockerfile
    container_name: auth-base-service
    secrets:
      - auth-key
    environment:
      - USER-BASE_ENDPOINT=user-base-service:50051
    ports:
      - "50055:50053"
    depends_on:
      - user-base-service

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80" 
    depends_on:
      - api-rest-gateway 

  db:
    image: postgres:16-alpine
    restart: always
    container_name: db
    secrets:
      - db-password
    volumes:
      - ./db/init-scripts:/docker-entrypoint-initdb.d
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=database
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    expose:
      - 5432
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  dbgate:
    image: dbgate/dbgate
    container_name: dbgate_ui
    restart: always
    ports:
      - "8080:3000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - dbgate_data:/root/.dbgate

volumes:
  db-data:
  dbgate_data:

secrets:
  db-password:
    file: db/password.txt
  auth-key:
    file: secrets/auth_key.txt
